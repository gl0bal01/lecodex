.DS_Store
node_modules
public
prof
tsconfig.tsbuildinfo
.obsidian
.quartz-cache
private/
.omc/
.replit
replit.nix

# plugins installed at build time by `npm run install-plugins`
.quartz/
quartz/cli/tui/dist/

# local plugin build output (`npm run build-plugins`)
plugins/*/dist/
plugins/*/node_modules/
plugins/*/package-lock.json

# content is fetched at build time from gl0bal01/intel-codex.
# Ignore the directory itself, not just its contents: locally it is a clone of
# that repo, and `git add -A` would otherwise stage it as a submodule gitlink.
/content/
